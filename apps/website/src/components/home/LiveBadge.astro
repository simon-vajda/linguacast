<span class="live-badge"><span class="dot pulse-live"></span>On air</span>

<style>
  .live-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-3);
    border-radius: var(--radius-full);
    background: var(--live-muted);
    color: var(--live-on-muted);
    font-size: var(--text-label);
    letter-spacing: var(--text-label--letter-spacing);
    font-weight: 600;
    text-transform: uppercase;
  }

  .dot {
    width: var(--size-dot);
    height: var(--size-dot);
    border-radius: var(--radius-full);
    background: var(--live);
    flex-shrink: 0;
  }
</style>
